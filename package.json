{
  "name": "offcourse-marketing",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "eleventy && node scripts/postbuild.mjs",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
